<%- include ('partials/head') %>

  <body class="min-width: 375px">
    <%- include ('partials/nav') %>
      

        <main class="container mb-5">
          <%- include ('partials/header') %>
          <div class="row justify-content-center pb-5 mx-auto" id="produtoContainer">
            <% for(let i=0; i < produtos.length; i++) {%>
              <div class="me-5 pb-3 col-sm-8 col-md-4 col-lg-2">
                <div class="card" id="card-product" style="width: 200px">
                  <img src="..." class="card-img-top" alt="...">
                  <div class="card-body text-center">
                    <h5 class="card-title">
                      <%= produtos[i].nome %>
                    </h5>
                    <p class="card-value">
                      <strong>R$: </strong>
                      <%= produtos[i].valor %>
                    </p>
                    <p class="card-text">
                      <%= produtos[i].descricao %>.
                    </p>
                    <p class="card-id">
                      <%= produtos[i].id %>.
                    </p>
                    <button class="btn btn-primary" type="submit" id="buyProduct" onclick="addCart({
                                    id: <%= produtos[i].id %>,
                                    title: '<%= produtos[i].nome %>',
                                    value: <%= produtos[i].valor %>,
                                    description: '<%= produtos[i].descricao %>'
                                  })">
                      Comprar
                    </button>
                  </div>
                </div>
              </div>
              <% }%>
          </div>
        </main>





        <hr class="mb-5">
        <%- include ('partials/footer') %>
          <%- include ('partials/scripts') %>
  </body>